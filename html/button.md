# Button Element

## Definition

The `<button>` element is used to create a clickable button on a webpage.

## Syntax

```html
<button>Click Me</button>
```

## Example

```html
<button>Submit</button>
```

## Important Points

- Used to perform an action when clicked.
- Can contain text, images, or icons.
- More flexible than the `<input>` button.